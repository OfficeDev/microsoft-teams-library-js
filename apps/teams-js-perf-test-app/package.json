{
  "name": "teamsjs-perf-test-app",
  "private": true,
  "author": "Microsoft teamsjs",
  "description": "teamsjs Perf Test App utilizing teamsjs App SDK to test teamsjs Hubs Perf",
  "version": "0.0.1",
  "scripts": {
    "build": "yarn eslint ./src/**/*.tsx && webpack",
    "clean": "rimraf ./build",
    "eslint-fix-build": "yarn eslint --fix --ext .tsx ./src && webpack",
    "start": "webpack serve"
  },
  "dependencies": {
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "@microsoft/teams-js": "file:../../packages/teams-js"
  }
}
