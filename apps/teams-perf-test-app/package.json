{
  "name": "teams-perf-test-app",
  "private": true,
  "author": "Microsoft Teams",
  "description": "Teams Perf Test App utilizing Teams JavaScript client SDK to test Teams Host Perf",
  "version": "0.0.1",
  "scripts": {
    "build": "pnpm lint && webpack",
    "clean": "rimraf ./build",
    "lint": "pnpm eslint ./src --max-warnings 0 --fix --ext .tsx",
    "start": "webpack serve"
  },
  "dependencies": {
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "@microsoft/teams-js": "workspace:*"
  }
}
