import React from 'react';
//import { call } from '@microsoft/teams-js';
//import { captureConsoleLogs } from './../components/sample/LoggerUtility';

const CallAPIs: React.FC = () => {
  /*
  const checkCallCapability = async () => {
    captureConsoleLogs((log) => console.log(log));
    console.log('Checking if Call module is supported...');
    const isSupported = call.isSupported();
    console.log(`Call module ${isSupported ? 'is' : 'is not'} supported`);
    return `Call module ${isSupported ? 'is' : 'is not'} supported`;
  };*/

  return (
    <div className="api-header">API: Call</div>
  );
};

export default CallAPIs;