import React from 'react';
//import { dialog } from '@microsoft/teams-js';

const DialogAPIs: React.FC = () => {
    /*
  const checkDialogCapability = async () => {
    console.log('Checking if Dialog module is supported...');
    const isSupported = dialog.isSupported();
    console.log(`Dialog module ${isSupported ? 'is' : 'is not'} supported`);
    return `Dialog module ${isSupported ? 'is' : 'is not'} supported`;
  };*/

  return (
    <div className="api-header">API: Dialog</div>
  );
};

export default DialogAPIs;
