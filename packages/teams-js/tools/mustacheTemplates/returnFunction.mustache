{{> functionComment}}
export function {{name}}({{> parameterList}}): Promise<{{returns}}> {
    {{> functionValidation}}

    return sendMessageToParentAsync(
        // TODO: Replace ApiName.YOUR_TELEMETRY_NAME_HERE with a reference to a new entry you have added to telemetry.ts
        getApiVersionTag({{capabilityName}}TelemetryVersionNumber, ApiName.YOUR_TELEMETRY_NAME_HERE),
        "{{capabilityName}}.{{name}}",
        [{{#dataForHost}}{{>dataForHost}}{{/dataForHost}}] // TODO: Verify this contains all data you need to pass to the host
    ){{>hostReturnHandling}}
}