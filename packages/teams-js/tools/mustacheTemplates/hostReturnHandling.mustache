.then(([{{#dataFromHost}}{{errorName}}{{#returnValue}}, {{returnValue.name}}{{/returnValue}}{{/dataFromHost}}]: [{{#dataFromHost}}SdkError{{#returnValue}}, {{returnValue.type}}{{/returnValue}}{{/dataFromHost}}]) => {
    {{#dataFromHost}}
    if ({{errorName}}) {
        throw new Error('Code ' + {{errorName}}.errorCode + {{errorName}}.message ? ': ' + {{errorName}}.message : '');
    }
    {{#returnValue.requiredForSuccess}}
    if (!{{name}}) {
        throw new Error('{{errorMessage}}');
    }
    {{/returnValue.requiredForSuccess}}
    {{#returnValue}}
    return {{name}};
    {{/returnValue}}
    {{/dataFromHost}}
});